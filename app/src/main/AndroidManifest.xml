<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.systemvx.androiddevtest">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidDevTest">


        <!--   OK   -->
        <activity android:name=".ui.chat.ChatActivity" />

        <!--  OK    -->
        <activity android:name=".ui.comment.CommentSendActivity" />
        <!--        -->
        <activity android:name=".ui.complaint.ComplaintSendActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.complaint.ComplaintProgress" />
        <activity android:name=".ui.credit.ConfidentialActivity" />
        <activity android:name=".ui.login.LoginActivity" />
        <activity android:name=".ui.main.MainActivity" />
        <activity android:name=".ui.orderbrowsing.OrderBrowsingActivity" />
        <activity android:name=".ui.orderdetail.OrderDetailActivity" />
        <activity android:name=".ui.search.SearchActivity" />
        <activity android:name=".ui.setting.SettingActivity" />
    </application>

</manifest>